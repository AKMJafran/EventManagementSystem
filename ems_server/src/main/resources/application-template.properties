spring.datasource.url=jdbc:mysql://localhost:3306/EMS_Database
spring.datasource.username=root
spring.datasource.password= your Password

jwt.secret=5534512798fe4e92dc34649a0a33d347

# ===============================
# File Server (PHP) integration
# ===============================
fileserver.url=http://localhost:8080/api
fileserver.client.name=test_backend
fileserver.client.secret=replace_with_real_secret

# Connection settings
fileserver.connect-timeout-ms=5000
fileserver.read-timeout-ms=15000

# Retry / token settings
fileserver.max-upload-attempts=2
fileserver.token-default-ttl-seconds=900
fileserver.token-refresh-skew-seconds=30

# Public access configuration (for generated file URLs)
fileserver.public-base-url=http://localhost:8081
fileserver.access-link-secret=replace_with_a_random_secret

# Event image upload constraints
fileserver.images.max-size=5MB